# Void Pointers

> A void pointer is a pointer that has no datatype
> A void pointer can be easily typecasted to any pointer type
> In simple language its a general purpose pointer variable

* Its not possible to dereference void pointers , we have to typecast it first

