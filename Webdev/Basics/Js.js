// Primitives are also an object in js because at run time primitives are also converted to objects in runtime so we can call methods on them

let num1 = 10;
let num2 = new Number(20)

console.log(num1)
console.log(num2)